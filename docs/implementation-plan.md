# 审计后实施计划

## 2026-09-06 增量实现与首轮发布

以下记录本轮实现范围，不替代生产部署与回滚验收。下方 2026-09-05 条目保留为历史记录，其当时的“未实现”或测试数量不代表当前工作区状态。

- 独立采集默认目标由 21 增至 23：增加 AikaShop，恢复 OTAOR。两处公开入口均已在本机及生产 VPS 只读验证；AikaShop 6 条报价库存未知，不参与起价/有效覆盖。OTAOR 41 条 SKU 中仅 1 条权益领取链接有货，不冒称完整订阅覆盖。
- 覆盖评估支持 CLI JSON/报告及每轮采集后的幂等 UTC 日账本。比较同产品规格、周期、币种、质保口径和新鲜在售报价。基线 stale/过期或直采整体过旧时不可判定。至少连续 7 日同范围有效组全覆盖且直采最低价不劣于基线，才建议人工复核；同日失败不能被晚间通过抹去。当前门槛未达标，保留 PriceAI 补充和全部历史，不自动关源。
- P0 来源与时效：由适配器赋值来源类型，`original_crawl` 是原店公开采集，不是商家主动接入的 `merchant_direct`；现有商家直连为 0。保留来源、域名级商家标识、报价更新时间、抓取核验时间及入库时间；不把第三方“官方价”转录冒充官方直连。writer 幂等迁移、旧库只读兼容、观察持续入库，包括相同价格的新核验及 A→B→A。
- P0 页面/商业化：来源标签和相对/绝对时间、统一跳转与最小点击统计、独立 Sponsor 模型及广告区、合作介绍/申请、商家认领申请入口。广告不影响自然排序，申请不自动认证或开权限。实际发布、真实商家接入及真实付费广告不是本轮本地测试可证明的结果。
- Mac 异机备份增加验证后白名单回执，服务器后台单列最近检查时间与新鲜度；不传档案、密钥或私人正文。回执失败保留本地有效版本并报异常；不能用服务器备份成功推导 Mac 已完成。超过 36 小时回执标为过期。
- P1 尚未做：完整商家认领验证、商家后台/手动报价/API、第一方占比 Dashboard、用户价格有效性反馈和专门的 30 日价格统计。P2 信用体系也未做。现有站长后台、第一方访问统计、历史曲线和同口径覆盖检查不等同于 P1 商家产品。

发布前只读核验：`/opt/linc/apps/price-radar/config.json` 不存在，原 21 目标来自旧代码默认值，并非显式配置阻止扩源。北京时间 14:22 左右已发布 `cbb21d5`，服务、隧道、备份定时器 active；线上 23 默认目标、直采提醒及新增列生效，无需创建或覆盖生产配置。其他环境若有显式目标数组，应备份后定点合并，不能覆盖自定义目标、通知凭据或整份配置。

首轮发布验收：132 项自动测试通过；CUA 本地桌面 1280×720、手机 390×844 验证首页、翻页、弹窗取消/Esc、认领表单隔离提交与 Sponsor 独立展示。公网首页、广告页、隐私、认领入口和 Sitemap 返回 200；未登录后台跳登录页；非法 `/go?url=` 返回 400，有效报价先确认再 302。核心文件线上 SHA256 与已提交代码一致。公网浏览器导航曾超时，生产可达性由真实 HTTP 和服务状态核验，不把它称为公网浏览器全流程验收。

Mac 14:28 完成新备份及回执，3 库隔离恢复通过。最后复核发现共享域无法唯一识别商家，追加按报价隔离计数和拒绝未归属 Sponsor 的修复；平台身份核验仍属于 P1。`www` DNS/跳转与百度控制台复查尚未完成，不能由网站成功发布推导这些外部项目完成。

验收参考：[覆盖政策](source-coverage-policy.md)、[扩源事实与限制](source-coverage-expansion.md)、[来源与历史语义](source-coverage-provenance.md)。本地测试与隔离验证必须由发布人重新执行，生产验收另行记录。

## 2026-09-05 历史计划与阶段记录

保持 Node SSR + SQLite，无收费依赖；生产投稿不作测试、不覆盖恢复。

1. A：一致性投稿备份、0600、保留 14 份、隔离恢复验证；测试 WAL 中未 checkpoint 的记录也被备份。
2. B：独立站长模块，默认关闭；scrypt、限流会话、CSRF、只读列表与追加操作审计；测试未认证、到期、退出与注入文本。
3. C：聚合请求统一边界、缓存超龄剔除与明确状态、Kami 分页/库存回归。
4. D：品牌与渠道正交，已证实域名归属，未知保留未知。
5. E：测试和语法门禁、发布前备份、已知版本回滚、HTTP 健康验证。

上线前核对凭据配置，不在聊天或仓库存管理员口令；异地备份另需目的地。

2026-09-05 本地验证：A–D 已实现，E 已建立测试/语法/备份/HTTP/旧代码恢复门禁；生产发布及真实回滚演练未执行。57 项测试通过，桌面 1440 和手机 390 的组合筛选、详情返回、HTTPS 后台登录/处理/退出已验证。后台默认关闭。

访问统计：现有代码支持 Cloudflare Web Analytics beacon，但本项目环境未配置，线上响应也未出现 beacon。不能据此推断云端无边缘历史。先核验现有 Cloudflare 账号只读分析权限及免费数据集能力，再复用数据；Visits 不等于独立访客，不将每日 UV 相加作为历史去重人数，不另建重复追踪，不补造历史。此项尚未实现。

后续授权更新：用户同意按方案实施并上线。因无已配置Cloudflare统计/只读权限，实施VPS第一方最小统计：7/30天服务器访问量、窗口去重访客估算、中文后台、授权CSV、公开隐私说明，31天去标识明细保留。62项测试通过，隔离HTTPS桌面1440/手机390已验证7/30天、空缺历史和CSV；临时进程全部退出。上线前运行两库备份，保留旧代码，不改动既有Tunnel。
