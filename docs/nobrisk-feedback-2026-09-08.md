# BriskAI 缺少商品反馈核查

- 反馈：`FB-20260908-W67TCU5T`，ChatGPT Plus，提交于 2026-09-08 10:50:22（北京时间）。
- 核查前生产库已为 `resolved`，12:29:53 的旧备注为“店铺已补货”；当前报价快照没有 `shop.nobrisk.com`，因此实际仍需补齐收录。
- 原站：`https://shop.nobrisk.com/`，名称 BriskAI。Mac 和生产 VPS 均成功读取无需登录的公开目录及商品页。
- 目录接口 `/user/api/index/commodity?limit=100&page=1` 的 `total=11`，但遍历有限分页实际获得 10 个唯一公开条目。以实际条目计数，不推断不可见商品。
- 三项 Plus：`/item/30` 菲卡充值月卡 ¥125，`/item/2` iOS 充值月卡 ¥130，`/item/16` 一年 Plus ¥1200；均公开标有库存。人工发货的大数库存仅视作在售标记，不声称真实存货量。
- 商品详情确认三项均为充值：月卡两渠道、年卡分组展示。iOS 月卡写明提前续费覆盖剩余时间，菲卡月卡写明不可提前续费，保留原商品条件。
- 店内 Pro 5x 普通目录标价 ¥700、`user_price=650`，使用普通挂牌价；Pro 20x ¥1200。所有商品不调用下单、支付或结算接口。
- Apple ID 虽在标题提到 GPT，但不属于 AI 订阅；仅采集 AI工具 分类。该分类 9 条中 1 条普通 Claude 账号售罄，3 条 Claude 订阅因说明包含“无售后”被现有保守过滤规则排除，不据此断言商家完全没有订阅质保；公开发布 5 条 ChatGPT 报价。

接入方式：固定 `nobrisk` 目标，复用 Kami 有限分页；在每轮中读取最多 20 个在售商品详情，按原商品 ID 和名称验证，提取“宝贝详情”区域，完整保留公开条件。仅在交付判断中移除本站固定的“常见封号原因”段落，避免把使用风险提及的共享当成商品交付。原段落仍保留在 `publicDescription`。

默认每 30 分钟刷新，沿用已有采集 daemon、缓存、新鲜度、售罄及无售后过滤和历史快照，不新增常驻进程。上线仅替换新适配器和登记表，保留其他任务正在发布的前端和审核改动。

验证：`npm run check` 全套语法检查及 407 项测试通过；生产 VPS 实测验证三项 Plus 的价格、月/年分类与代充交付。
