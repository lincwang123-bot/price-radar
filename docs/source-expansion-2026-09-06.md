# 16688、链动与原店补充采集

日期：2026-09-06。范围是公开价格、商品说明、库存状态与原店链接；无登录、下单、账号凭据或防护绕过。不是全网完整性声明，也不是商家履约认证。

## 改动前基线

生产行情库只读检查时间：2026-09-06T08:46:49.039Z。

- 默认原店目标 23 个。
- 按实际网页目录、有效在售、去重口径，共 297 条商店报价：独立站 213、链动 84、16688 0。官方/API参考不计入该口径。
- ChatGPT Plus 33 条（链动 1），Claude Pro 98 条（链动 83）。
- 当时 PriceAI 原始快照含 42 条 16688 报价，但源端 `stale=true` 导致整批退出当前在售报价；未把该标记改成新鲜。

## 16688：新增 30 家原店独立采集

从已有公开商品链接读取商品所属店铺，再核对店铺公开列表，不枚举店号。种子 URL、店号和名单固化在 `collectors/direct/platform16688.mjs` 的 `PLATFORM16688_SHOPS`。

依据：[原站商品页](https://16688.com.cn/goods/G44611111) 引用的公开脚本 `index.4f7c8a9b.js`、`index.dfe4ccba.js`。前端使用无登录 POST `/shopApi/goods/detail`、`/shopApi/shop/detail` 和 `/shopApi/goods/list`。采集仅使用后一只读目录接口，固定参数 `shop_no`、空商品分类/关键词、`sort=default`，不调用购买或结算接口。

30 家列表均在本机与生产 VPS 验证 HTTP 200、业务 `code=1`。生产在 09:02 UTC 读取 `https://www.16688.com.cn/robots.txt` 返回 HTTP 200、空文本，没有声明禁止路径；这不被解释为商家背书或任何交易授权。探测时：

| 口径 | 数量 |
| --- | ---: |
| 原站返回 SKU | 532 |
| 售罄 | 102 |
| 标题或说明明确无质保 | 206 |
| 说明过滤后的规范化记录 | 326 |
| 分类并通过售罄/无保障过滤 | 115 |
| 其中有明确在售证据 | 106 |
| 其中库存未知，不进入在售排行 | 9 |

售罄与无质保计数有重叠。以上为探测时新采集器自身结果，不能直接与既有聚合报价相加作为网页总数。店号来自商品公开响应，不把 30 家共享域名店铺算成 30 个独立域名。

每店至少间隔 60 分钟；目录超过上限、显式返回不完整目录、业务错误或跨店身份不一致即失败，不将其伪装成空店。未知库存不推断为有货。`www.16688.com.cn` 与裸域名正确归入16688，任意未知子域不自动归入。

同一 origin 遇到 HTTP 401/403/407/429 或明确防护/认证挑战时，本轮停止后续同域店铺请求；不同原店域名继续，普通网络错误不误作平台禁止访问。LDXP 摘要接口明确拒绝时，也不继续查询商品。精确的16688单商品 URL 按商品 ID 与币种去重，优先直采，避免旧聚合标题变更后重复列出；有意义的规格查询参数和其他平台多 SKU 规则仍保留。

## 链动：扩大有界补充，不冒充原店直采

本机 `wzyp.cn` 原站 ShopAPI 正常，生产同一入口返回 HTTP 200 的防护 HTML，包含 `x-tengine-error: denied by http_custom`。商家公开入口 `pay.ldxp.cn/shop/dabai` 在生产也遇到防护。因此既有 5 个原店目标仍不默认启用，没有切换代理或绕过。

现有 RelayWatch 补充查询依据[官方开源接口实现](https://github.com/bnlbnf/ai-cardnav)：`/api/ldxp/goods` 支持 `stock=in`、`sort=price_asc`、`page` 与 `page_size`。

- 3 个关键词扩为 12 个，覆盖 GPT、Claude、Gemini、Grok、Cursor、Perplexity、Codex、邮箱与接码。
- 默认每关键词最多 2 页，每页 100 条；整轮最多 24 次商品请求，加一次摘要请求；间隔至少 1 秒，整轮至少间隔 30 分钟。
- 页间去重，重复页、空页、达到预算均停止；不把有限样本声称为全平台完整目录。
- `coverage:ldxp-goods` 持久化逐词总量、页数、重叠口径、独立收录量和截断原因，供内部检查。
- 仍保留真实第三方来源。兑换链接、API 额度及未知商品不凭搜索词冒充完整订阅。

## AI补给站：新增独立店铺

[公开目录](https://aichong.xin/api/products) 与[本地购买页](https://aichong.xin/buy.html?id=1) 使用同一数据；目录明确 `self_pay=true`，不沿用首页历史链动入口作为当前成交链接。读取前检查 [robots](https://aichong.xin/robots.txt)。

本机与 VPS 均可读取：17 个原始商品，16 个可分类，14 条可分类且明确在售。2 件售罄排除，Claude KYC 未可靠分类。按 60 分钟间隔采集。只读当前挂牌价，不用划线官方价、宣传折扣或臆测库存。

同时修复“充值到自己账号”被误判成售卖成品账号：以明确交付描述判定，保留成品号、共享和充值教程的反例测试。

## 暂未接入与后续边界

- `catcard.uk`：robots 禁止 `/api/`，没有改走被禁止的目录接口。
- `kbshub.store`：防护挑战，停止。
- `xtacc.top`：现有 Acc-OTAOR 的旧地址，不重复统计新店。
- `catfk.com`：已找到公开商品入口，但未确认可靠完整目录接口，本轮不默认接入。
- 链动原站直采：需站方提供允许服务器访问的公开接口或授权白名单，不能仅因本机能访问就声称生产可稳定直采。

默认配置统一复用 registry：23 个原目标 + 30 家16688店铺 + 1 家AI补给站 = 54 个目标。保留原有失败隔离、过期退出、去重、分页和历史，不新增付费服务，不删除历史记录。随着报价扩大，行情库及全量历史备份会增长；本轮没有擅自缩短保留期。

## 扩量后的生产验收修正

首轮生产更新后，逐项比较数据库与网页最终目录，发现并补测两类问题：

- 镜像 Plus 服务、明确出售的 AI 充值余额归入 API / CDK / 额度，不混作原厂订阅。完整数字边界防止 `1.5x`、`15x` 等误认成 `5x`；“余额支付/余额不足”这样的结算提示不改变原厂订阅的类别。
- 已经能明确分类的 `G Plus`、`Codex Plus/Pro` 和接码商品，不再被二次裸套餐推断的英文白名单误丢。混合品牌套餐仍排除，未知品牌不凭搜索词推断。

生产统计以过滤、去重后的实际网页报价为准，不以原始抓取行数为准；新增采集任务已纳入原有定时更新，受防护或未确认的来源仍不声称已覆盖。
